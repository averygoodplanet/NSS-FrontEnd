extends ../template/layout

block content
  .row
    .small-12.columns
  .row
    .small-12.columns
      #artists
        each artist in artists
          .artist
            a(href='/artists/#{artist.id}')
              img(src=artist.photo)
            h6.subheader= artist.name